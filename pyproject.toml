[project]
name = "mlops"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mlflow>=3.16.1",
    "torch>=2.14.0",
    "transformers>=5.17.0",
]
